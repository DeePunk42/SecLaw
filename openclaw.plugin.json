{
  "id": "seclaw",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "llm": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "model": { "type": "string", "description": "Model identifier. Use 'providerName/modelId' format to auto-resolve from openclaw.json models.providers, or a plain model name with endpoint/apiKey for legacy mode." },
          "enabled": { "type": "boolean" },
          "maxConcurrent": { "type": "number" },
          "endpoint": { "type": "string", "description": "Legacy fallback: direct LLM endpoint URL. Not needed when using providerName/modelId format." },
          "apiKey": { "type": "string", "description": "Legacy fallback: API key for the direct endpoint. Not needed when using providerName/modelId format." },
          "promptRecentCalls": { "type": "number" },
          "trustedSenderLabels": { "type": "array", "items": { "type": "string" } },
          "retry": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "maxRetries": { "type": "number" },
              "initialBackoffMs": { "type": "number" },
              "cooldownMs": { "type": "number" },
              "cooldownThreshold": { "type": "number" }
            }
          }
        }
      },
      "timeouts": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "syncAuditMs": { "type": "number" },
          "asyncAuditMs": { "type": "number" },
          "syncTimeoutPolicy": { "type": "string", "enum": ["fail_closed", "fail_open"] }
        }
      },
      "logging": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "level": { "type": "string", "enum": ["debug", "info", "warn", "error"] },
          "auditJsonl": { "type": "boolean" }
        }
      },
      "rules": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "extra": { "type": "array" }
        }
      },
      "dashboard": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": { "type": "boolean" },
          "port": { "type": "number" },
          "host": { "type": "string" }
        }
      },
      "agentProfiles": {
        "type": "object",
        "additionalProperties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "classification": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "safeCommands": { "type": "array", "items": { "type": "string" } }
              }
            }
          }
        }
      }
    }
  }
}
